const path = require('path');

module.exports = {
    'config': path.resolve('./src/config', 'config.json'), // cấu hình 
    'migrations-path': path.resolve('./src', 'migrations'), // tạo bảng đồ trong hô hình mvc 
    'models-path': path.resolve('./src', 'models'), // tạo mô hình 
    'seeders-path': path.resolve('./src', 'seeders') // tạo dữ liệu fake 
}